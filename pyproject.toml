[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sungrid_core"
version = "0.1.0"
description = "Engineering Logic Core for SunGrid.ai"
dependencies = []
